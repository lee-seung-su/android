- CALL_PHONE, INTERNET은 manifest에 권한 부여해야한다

-viewpager는 한번에 선택된화면 + 왼쪽화면 + 오른쪽화면을 스택(?)에 올려서 동작하므로
getitem부분에서 postion가지고 같은화면 또 return할 수 없다. 즉 return fragmentList.get(1)은 무조건 1번만 가능
무한 viewpager만드려면 getSize를 크게한다음 getitem에서 return fragment_1()이렇게 객체 반환하면된다 일단은 된다
메모리는 잘 모름

webViewer쓸 때 webClient()뭐 이런거 써야되는데 나중에는 접속이 안 될 떄가 있다.
그러면 manifest파일에서 <application>에 android:usesCleartextTraffic="true" 이거 써주면된다

추가로 텍스트쓸때 키보드창에 입력창이 가려지면
manifest에서 <activity>에 android:windowSoftInputMode="adjustPan"써주면 자동으로 버튼이 위로 올라간다

intent로 call, sms보낼떄 call, dial은 "tel:010~"이런식으로 tel 붙여줘야하고
sms는 "smsto:010~"이렇게 smsto붙여야한다.

fragment로 viewpager만들려면
fraglist list만들어서
리스트에 각 fragment객체 생성하고
이거를 FragmentPagerAdapter상속받은 adapter의 데이터와 연결하고
viewpager와 adapter연결하면 된다
customAdapter에서 itemCount, getItem 2개만 override하면되고
tablayout만들고싶으면 layout에 추가한다음 customAdapter에서
getTitle override해주면 된다

view로 viewpager만들려면
각 layout res에 만든다음에
각 layout 객체화해주는 class만든다음 addView로 자기자신view에 더하기?
마찬가지로 main에서 이러한 객체생성한 리스트 만든다음
adapater의 데이터와 연결하고 viewpager와 adapter연결하면된다
이떄 customAdapter는 itemCount, initiateItem(이게 getitem이다)
destroyitem, 마지막으로 isViewFromObject? 이런거 override해주면된다

마시멜로우(and 6.0)이후로는 위험권한 manifest에 등록해도 코드로 한번더 확인해야된다
ex) camera, call
- 권한 등록 후 확인
var PERMISSIONS = ContextCompat.Manifest.permissions.CAMERA
if PERMISSIONS == PackageManger.PERMISSIONS_GRANTED이면 코드 실행
else request()함수 만든다음
request안에서 ActivityCompat.requestPermissions(this, arrayOf(Manifest.permissions.CAMERA), 11) // 11은 임의 번호
onRequestPermissionResult overried한다음에 super부분 지워준다음
requestCode가 11이고 grantResult[0] == PackageManger.PERMISSIONS_GRANTED면 함수실행.
이런식으로 하면된다. OnRequestPermissionResult는 따로 override 안해줘도된다.
arrayOf는 만약 PERMISSIONS가 String 리스트일떄 사용되는거같다. maybe~